name: transcribe
description: "A Flutter app for converting speech to text"
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.4.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter
  build_runner: ^2.3.3
  flutter_lints: ^3.0.0
  import_sorter: ^4.6.0

flutter:
  uses-material-design: true
